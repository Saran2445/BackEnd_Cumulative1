@model List <Cumulative1.Models.Student>
@{
    ViewBag.Title = "List Of Students";
}

<h2>List of Students</h2>

@foreach (var student in Model)
{
    <div><a href="Show/@student.StudentId">@student.StudentFullName</a></div>
}

